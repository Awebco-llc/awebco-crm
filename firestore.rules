rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
      // Secure access: Only authenticated team members can read/write.
      allow read, write: if request.auth != null;
    }
  }
}
